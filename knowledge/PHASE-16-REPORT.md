# Phase 16 — Full Knowledge Graph & Live Evidence Reconstruction

**Date:** 2026-08-11  
**Status:** COMPLETE for the local canonical data-layer reconstruction  
**UI_CHANGE_REQUIRED:** FALSE  
**UI_UNCHANGED:** TRUE

## Executive result

The existing explorer remains the presentation layer. A canonical semantic layer was added under `knowledge/` and generated from the complete local Ix-findings evidence universe, including phase artifacts, registries, packets, reports, graph fragments, GitHub manifest data, and the Phase 15 audit.

The generator ingested **344 local source files** and produced:

- **549 canonical entities**;
- **931 typed relationships**;
- **0 dangling relationships**;
- **0 orphan entities**;
- **6 explicit contradiction records**;
- **86.7% finding evidence coverage**;
- **100% human-summary coverage**;
- **100% LLM metadata coverage**.

The 86.7% evidence figure is intentionally not inflated: some historical findings and operational records do not have direct evidence links and remain visible as limited-provenance entities.

## Current live baseline used

A fresh read-only GitHub check confirmed:

- upstream `ix-infrastructure/Ix` default branch is `main`;
- current upstream HEAD is `1292375548fb8f4431ac5afc34c68fe2573434d1`;
- open PRs are #395, #393, and #388;
- open issues are #385, #383, #349, and #219;
- old manifest states for #390, #391, and #392 are retained as historical metadata, while the canonical current state marks them resolved.

## Canonical files

- `ontology.json` — entity, edge, evidence, status, confidence, temporal, and privacy contracts;
- `manifest.json` — source hierarchy, regeneration rules, live baseline, and counts;
- `build-knowledge.mjs` — deterministic, no-network, Node-built-in generator;
- `entities.json`, `relationships.json`, `evidence.json`, `sources.json`;
- `timelines.json`, `phases.json`, `decisions.json`, `suggestions.json`;
- `tests.json`, `security.json`, `agents.json`, `contradictions.json`;
- `aliases.json`, `indexes.json`, `snapshots.json`;
- `llm/manifest.json`, `llm/entity-index.json`, `llm/traversal-index.json`;
- `derived/ui-data.json` — canonical graph projection for tooling;
- `ui-compat-adapter.mjs` — read-only adapter used by both explorer data generators;
- `DATA-QUALITY-REPORT.md` and `data-quality.json`;
- `KNOWLEDGE-GRAPH-README.md` and `KNOWLEDGE-GRAPH-LLM-GUIDE.md`.

## Important reconciliations

1. F-008/F-009 remain historically preserved but current state is resolved by upstream work.
2. Phase 14's MCP “PR-ready” conclusion is preserved as history; Phase 15's `CONTRIBUTE_AFTER_REWORK` state is current for the knowledge model.
3. Remap PR #393 remains the existing open contribution, with the Phase 15 lifecycle/concurrency concerns attached; no second PR or branch was created.
4. The Pages and wiki datasets are explicitly modeled as snapshots, not current GitHub truth. Both generators now read the canonical layer through the adapter while preserving the existing `window.IX_DATA` contract.
5. Compass source inaccessibility remains a blocker; no inaccessible source or root cause was fabricated.
6. Phase 13's “terminal/no Phase 14” statement is historical because later Phase 14 and Phase 15 artifacts exist.

## Validation

Executed successfully:

```text
node knowledge/build-knowledge.mjs
JSON validation: passed
entity/relationship endpoint validation: passed
orphan validation: passed
traversal anchor validation: passed
secret-pattern scan: no matches
```

The traversal anchors for F-009, PR #393, and the fork MCP implementation resolve through evidence, issues/PRs, branches, commits, packets, tests/security records, and source provenance.

## Parasite-skill routing

The installed parasite-skill system was scanned and validated:

- 88 skills scanned;
- 0 skill-spec issues;
- selected intelligence workflow centered on `ix`, `graphify`, `system-connector`, `gitingest`, and parasite-skill routing;
- route/plan artifacts were generated by the local skill registry.

The first shell invocation from the alternate working directory failed to spawn Git Bash; the same commands were rerun from the active workspace using absolute script paths and completed successfully.

## Explicit non-actions

- no upstream commits;
- no upstream push;
- no fork push;
- no PR creation;
- no issue creation;
- no issue comments;
- no reviews;
- no maintainer contact;
- no Pages deployment;
- no implementation-repository edits;
- no UI redesign, asset change, or interaction change; the data generators now use a compatibility adapter;
- no deletion of historical artifacts.

## Remaining limitations

- The private `system-compass` source remains inaccessible.
- Live GitHub data is represented from the read-only baseline recorded here; rerun the generator before any future publication or contribution decision.
- The internal canonical layer contains provenance and historical context; it must not be published directly. Use the existing allowlist-based Pages projection for any separately authorized public snapshot. Regenerate both projections after canonical changes with the commands documented in `knowledge/KNOWLEDGE-GRAPH-README.md`.
